local Keys = {
    SPACE = "space",
    LEFT = "left",
    RIGHT = "right",
}
local Bindings = {
    Jump = Keys.SPACE,
    Left = Keys.LEFT,
    Right = Keys.RIGHT
}

local keyboard =  {Keys = Keys, Bindings = Bindings}
return keyboard
